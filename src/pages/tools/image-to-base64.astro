---
import ToolLayout from "../../layouts/ToolLayout.astro";
import ToolShell from "../../components/ToolShell.astro";

const title = 'Image to Base64';
const description = 'Konversi gambar ke Base64 online. Upload gambar dan dapatkan Base64 string untuk CSS/HTML.';
const keywords = 'image to base64, base64 image, convert image, img to base64';
const aboutText = 'Image to Base64 Konversi gambar ke Base64 online. Upload gambar dan dapatkan Base64 string untuk CSS/HTML.. Alat ini berjalan 100% di browser, data tidak dikirim ke server.';
const relatedTools = [
  { slug: 'image-resizer', name: 'Image Resizer', icon: '🖼' },
  { slug: 'color-picker', name: 'Color Picker', icon: '🎨' },
  { slug: 'hash-generator', name: 'Hash Generator', icon: '#' },
  { slug: 'qr-generator', name: 'Qr Generator', icon: '▐▌' }
];

---

<ToolLayout
  title={title}
  description={description}
  keywords={keywords}
>

<div class="flex flex-wrap gap-4">
        <div class="lg:w-5/12 w-full">
          <div class="drop-zone" id="dropZone">
            <div class="mb-2">
              <svg width="48" height="48" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path d="M12 16V4m0 0L8 8m4-4l4 4M4 16v2a2 2 0 002 2h12a2 2 0 002-2v-2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </div>
            <p class="mb-1 font-semibold">Drag & drop gambar di sini</p>
            <p class="opacity-60 small mb-3">atau</p>
            <label class="btn btn-primary btn-sm">
              Pilih File
              <input type="file" id="fileInput" accept="image/*" hidden>
            </label>
            <p class="opacity-60 small mt-2 mb-0">Format: PNG, JPG, GIF, WebP, SVG</p>
          </div>
          <div id="info" class="text-sm opacity-60 mt-2"></div>
        </div>
        <div class="lg:w-7/12 w-full">
          <div id="preview" class="mb-2"></div>
          <div class="flex justify-between items-center mb-1">
            <label class="label-text font-semibold mb-0">Base64 Data URI</label>
            <div>
              <button id="btnCopy" class="btn btn-sm btn-outline-secondary">Copy</button>
              <button id="btnClear" class="btn btn-sm btn-outline-danger ms-1">Clear</button>
            </div>
          </div>
          <textarea id="outputText" class="input input-bordered w-full" readonly placeholder="Hasil Base64 akan muncul di sini..."></textarea>
        </div>
      </div>
    `n

<ToolShell about={aboutText} />

</ToolLayout>

<style is:inline>
.saweria-float { position: fixed; bottom: 24px; right: 24px; z-index: 9999; background: #ff6b6b; color: #fff; border-radius: 50px; padding: 10px 20px 10px 16px; text-decoration: none; font-size: 14px; font-weight: 600; display: flex; align-items: center; gap: 8px; box-shadow: 0 4px 15px rgba(255,107,107,0.4); transition: all 0.3s ease; font-family: "Inter",sans-serif; }
.saweria-float:hover { transform: translateY(-3px); box-shadow: 0 6px 20px rgba(255,107,107,0.6); color: #fff; }
.saweria-float svg { animation: pulse 1.5s ease infinite; }
@keyframes pulse { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.15); } }
@media (max-width: 576px) { .saweria-float { bottom: 16px; right: 16px; padding: 8px 14px 8px 10px; font-size: 12px; } }
</style>

<script is:inline>
</script>

<script is:inline>
var dropZone = document.getElementById('dropZone');
var fileInput = document.getElementById('fileInput');
var preview = document.getElementById('preview');
var output = document.getElementById('outputText');
var info = document.getElementById('info');

function handleFile(file) {
  if (!file) return;
  if (!file.type.startsWith('image/')) {
    alert('File harus berupa gambar');
    return;
  }

  var reader = new FileReader();
  reader.onload = function(e) {
    var dataUri = e.target.result;
    output.value = dataUri;
    preview.innerHTML = '<img src="' + dataUri + '" alt="preview">';
    var sizeKB = (file.size / 1024).toFixed(1);
    var base64Len = dataUri.length;
    info.textContent = file.name + ' | ' + sizeKB + ' KB | Base64: ' + base64Len + ' chars';
  };
  reader.readAsDataURL(file);
}

dropZone.addEventListener('dragover', function(e) { e.preventDefault(); this.classList.add('dragover'); });
dropZone.addEventListener('dragleave', function() { this.classList.remove('dragover'); });
dropZone.addEventListener('drop', function(e) {
  e.preventDefault();
  this.classList.remove('dragover');
  if (e.dataTransfer.files.length > 0) handleFile(e.dataTransfer.files[0]);
});
dropZone.addEventListener('click', function() { fileInput.click(); });
fileInput.addEventListener('change', function() { if (this.files.length > 0) handleFile(this.files[0]); });

document.getElementById('btnCopy').addEventListener('click', function() {
  if (!output.value) return;
  navigator.clipboard.writeText(output.value).then(function() {
    var btn = document.getElementById('btnCopy');
    btn.textContent = 'Copied!';
    setTimeout(function() { btn.textContent = 'Copy'; }, 1500);
  });
});
document.getElementById('btnClear').addEventListener('click', function() {
  output.value = '';
  preview.innerHTML = '';
  info.textContent = '';
  fileInput.value = '';
});
</script>


---
import ToolLayout from "../../layouts/ToolLayout.astro";
import ToolShell from "../../components/ToolShell.astro";

const title = 'Palindrome Checker';
const description = 'Cek palindrome online. Periksa apakah kata, kalimat, atau angka adalah palindrome.';
const keywords = 'palindrome checker, palindrome check, cek palindrome, palindrome test';
const aboutText = 'Palindrome Checker Cek palindrome online. Periksa apakah kata, kalimat, atau angka adalah palindrome.. Alat ini berjalan 100% di browser, data tidak dikirim ke server.';
const relatedTools = [
  { slug: 'anagram-finder', name: 'Anagram Finder', icon: '🔀' },
  { slug: 'reverse-text', name: 'Reverse Text', icon: '↩' },
  { slug: 'word-frequency', name: 'Word Frequency', icon: '📊' },
  { slug: 'textcase', name: 'Textcase', icon: 'Aa' }
];

---

<ToolLayout
  title={title}
  description={description}
  keywords={keywords}
>

<header class="flex items-center gap-3 mb-4"><div class="tool-icon" style="background:#f0f9ff;color:#0891b2;">Aa</div><div><h1 class="h3 mb-0">Palindrome Checker</h1><p class="opacity-60 small mb-0">Periksa apakah kata, kalimat, atau angka adalah palindrome (dibaca sama dari depan dan belakang).</p></div></header>
<div class="flex flex-wrap gap-4"><div class="col-lg-8">
<div class="card card-bordered bg-base-100 p-3">
<textarea id="textInput" class="input input-bordered w-full font-mono mb-3" rows="3" placeholder="Masukkan teks..." autofocus>Kasur ini rusak</textarea>
<div class="flex gap-2 flex-wrap mb-3">
<button id="btnCheck" class="btn btn-primary">Cek Palindrome</button>
<button id="btnClear" class="btn btn-outline-danger">Clear</button>
</div>
<div class="flex items-center gap-2 form-switch mb-3"><input type="checkbox" id="caseSensitive" class="checkbox"><label class="flex items-center gap-2-label small" for="caseSensitive">Case sensitive (beda huruf besar/kecil dianggap beda)</label></div>
<div id="resultArea" class="p-4 rounded text-center" style="display:none"><div class="display-3 mb-2" id="resultIcon">?</div><div class="h4 font-bold" id="resultText"></div><div class="text-sm opacity-60" id="resultDetail"></div></div>
</div></div>
<div class="col-lgap-4">
<div class="card card-bordered bg-base-100 p-3"><h6 class="mb-3">Contoh Palindrome</h6>
<div class="small" style="line-height:2">
<div style="cursor:pointer" class="example">Kasur ini rusak</div>
<div style="cursor:pointer" class="example">Ibu Ratna antar ubi</div>
<div style="cursor:pointer" class="example">Kakak</div>
<div style="cursor:pointer" class="example">Aku suka rajawali, bapak? Apakah jawab? Aku suka rajawali, ibu?</div>
<div style="cursor:pointer" class="example">12321</div>
<div style="cursor:pointer" class="example">racecar</div>
</div></div>
</div></div>

<ToolShell about={aboutText} />

</ToolLayout>

<style is:inline>
.saweria-float { position: fixed; bottom: 24px; right: 24px; z-index: 9999; background: #ff6b6b; color: #fff; border-radius: 50px; padding: 10px 20px 10px 16px; text-decoration: none; font-size: 14px; font-weight: 600; display: flex; align-items: center; gap: 8px; box-shadow: 0 4px 15px rgba(255,107,107,0.4); transition: all 0.3s ease; font-family: "Inter",sans-serif; }
.saweria-float:hover { transform: translateY(-3px); box-shadow: 0 6px 20px rgba(255,107,107,0.6); color: #fff; }
.saweria-float svg { animation: pulse 1.5s ease infinite; }
@keyframes pulse { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.15); } }
@media (max-width: 576px) { .saweria-float { bottom: 16px; right: 16px; padding: 8px 14px 8px 10px; font-size: 12px; } }
</style>

<script is:inline>
document.getElementById('year').textContent=new Date().getFullYear();
</script>

<script is:inline>
var inp=document.getElementById('textInput'),res=document.getElementById('resultArea'),icon=document.getElementById('resultIcon'),txt=document.getElementById('resultText'),det=document.getElementById('resultDetail'),cs=document.getElementById('caseSensitive');
function check(){
  var t=inp.value.trim();if(!t){res.style.display='none';return}
  var clean=cs.checked?t:t.toLowerCase().replace(/[^a-z0-9]/g,'');
  var rev=clean.split('').reverse().join('');
  var isPal=clean===rev;
  res.style.display='block';
  icon.textContent=isPal?'?':'?';icon.style.color=isPal?'#22c55e':'#ef4444';
  txt.textContent=isPal?'Palindrome!':'Bukan Palindrome';
  txt.style.color=isPal?'#22c55e':'#ef4444';
  det.innerHTML='<span class="opacity-60">Setelah difilter: "'+clean+'"<br>Dibalik: "'+rev+'"</span>';
}
document.getElementById('btnCheck').addEventListener('click',check);
document.getElementById('btnClear').addEventListener('click',function(){inp.value='';res.style.display='none'});
inp.addEventListener('input',check);cs.addEventListener('change',check);
document.querySelectorAll('.example').forEach(function(el){
  el.addEventListener('click',function(){inp.value=this.textContent;check()});
});
inp.value='Kasur ini rusak';check();
</script>


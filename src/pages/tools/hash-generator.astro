---
import ToolLayout from "../../layouts/ToolLayout.astro";
import ToolShell from "../../components/ToolShell.astro";

const title = 'Hash Generator';
const description = 'Generate hash MD5, SHA1, SHA256, SHA512 online. Hash generator gratis untuk developer.';
const keywords = 'hash generator, md5, sha1, sha256, sha512, hash online';
const aboutText = 'Hash Generator Generate hash MD5, SHA1, SHA256, SHA512 online. Hash generator gratis untuk developer.. Alat ini berjalan 100% di browser, data tidak dikirim ke server.';
const relatedTools = [
  { slug: 'uuid-generator', name: 'Uuid Generator', icon: '🆔' },
  { slug: 'base64-coded', name: 'Base64 Coded', icon: 'B64' },
  { slug: 'password-generator', name: 'Password Generator', icon: '🔑' },
  { slug: 'jwt-decoder', name: 'Jwt Decoder', icon: 'JWT' }
];
const bodyHtml = `<!-- Navbar khusus mobile -->
      

      

      <!-- Form -->
      <div class="mb-4">
        <div class="mb-3">
          <label for="text" class="label-text">Input Text</label>
          <textarea id="text" rows="3" class="input input-bordered w-full" placeholder="Masukkan teks untuk di-hash"></textarea>
        </div>
        <button type="button" id="generateBtn" class="btn btn-primary">Generate</button>
      </div>

      <!-- Hasil -->
      <div id="results" style="display: none;">
        <div class="card card-bordered bg-base-100 p-3">
          <h5 class="mb-3">Hasil Hash</h5>
          <div class="overflow-x-auto">
            <table class="table table-bordered table-sm align-middle">
              <thead>
                <tr>
                  <th>Algoritma</th>
                  <th>Hash</th>
                  <th>Aksi</th>
                </tr>
              </thead>
              <tbody id="hashTable"></tbody>
            </table>
          </div>
        </div>
      </div>
    `;
---

<ToolLayout
  title={title}
  description={description}
  keywords={keywords}
>

<Fragment set:html={bodyHtml} />

  <ToolShell about={aboutText} />

</ToolLayout>

<style is:inline>
.saweria-float { position: fixed; bottom: 24px; right: 24px; z-index: 9999; background: #ff6b6b; color: #fff; border-radius: 50px; padding: 10px 20px 10px 16px; text-decoration: none; font-size: 14px; font-weight: 600; display: flex; align-items: center; gap: 8px; box-shadow: 0 4px 15px rgba(255,107,107,0.4); transition: all 0.3s ease; font-family: "Inter",sans-serif; }
.saweria-float:hover { transform: translateY(-3px); box-shadow: 0 6px 20px rgba(255,107,107,0.6); color: #fff; }
.saweria-float svg { animation: pulse 1.5s ease infinite; }
@keyframes pulse { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.15); } }
@media (max-width: 576px) { .saweria-float { bottom: 16px; right: 16px; padding: 8px 14px 8px 10px; font-size: 12px; } }
</style>

<script is:inline>
function generateHashes() {
  const text = document.getElementById('text').value;
  if (!text.trim()) {
    alert('Masukkan teks terlebih dahulu');
    return;
  }

  const hashes = {
    'MD5': CryptoJS.MD5(text).toString(),
    'SHA1': CryptoJS.SHA1(text).toString(),
    'SHA256': CryptoJS.SHA256(text).toString(),
    'SHA512': CryptoJS.SHA512(text).toString(),
  };

  const tbody = document.getElementById('hashTable');
  tbody.innerHTML = '';

  Object.entries(hashes).forEach(([algo, hash]) => {
    const row = document.createElement('tr');
    row.innerHTML = \`
      <td><strong>\${algo}</strong></td>
      <td><code id="hash_\${algo}" style="word-break: break-all;">\${escapeHtml(hash)}</code></td>
      <td>
        <button type="button" class="btn btn-sm btn-outline-secondary" onclick="copyHash('hash_\${algo}')">Copy</button>
      </td>
    \`;
    tbody.appendChild(row);
  });

  document.getElementById('results').style.display = 'block';
}

function copyHash(id) {
  const text = document.getElementById(id).innerText;
  navigator.clipboard.writeText(text).then(() => {
    const btn = document.querySelector(\`[onclick="copyHash('\${id}')"]\`);
    if (!btn) return;
    const oldText = btn.textContent;
    btn.textContent = '? Copied!';
    setTimeout(() => { btn.textContent = oldText; }, 2000);
  }).catch(() => {
    alert('Gagal menyalin');
  });
}

function escapeHtml(text) {
  const div = document.createElement('div');
  div.textContent = text;
  return div.innerHTML;
}

document.getElementById('generateBtn').addEventListener('click', generateHashes);
document.getElementById('text').addEventListener('keypress', (e) => {
  if (e.key === 'Enter' && e.ctrlKey) {
    generateHashes();
  }
});
</script>


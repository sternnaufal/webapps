---
import ToolLayout from "../../layouts/ToolLayout.astro";
import ToolShell from "../../components/ToolShell.astro";

const title = 'URL Encode / Decode';
const description = 'Encode dan decode URL online gratis. URL encoder dengan dukungan RFC 3986.';
const keywords = 'url encode, url decode, url encoder, url decoder, encode url';
const aboutText = 'URL Encode / Decode Encode dan decode URL online gratis. URL encoder dengan dukungan RFC 3986.. Alat ini berjalan 100% di browser, data tidak dikirim ke server.';
const relatedTools = [
  { slug: 'base64-coded', name: 'Base64 Coded', icon: 'B64' },
  { slug: 'hash-generator', name: 'Hash Generator', icon: '#' },
  { slug: 'html-entity', name: 'Html Entity', icon: '&' },
  { slug: 'slug-generator', name: 'Slug Generator', icon: '🔗' }
];

---

<ToolLayout
  title={title}
  description={description}
  keywords={keywords}
>

<div class="card card-bordered bg-base-100 p-4 shadow-sm">
        <div class="mb-3">
          <label for="inputText" class="label-text">Input</label>
          <textarea id="inputText" class="input input-bordered w-full" placeholder="Masukkan teks atau URL encoded string...">https://example.com/search?q=hello world&lang=id&page=1</textarea>
        </div>
        <div class="flex gap-2 flex-wrap mb-3">
          <button id="btnEncComponent" class="btn btn-success">encodeURIComponent</button>
          <button id="btnEncURI" class="btn btn-outline-success">encodeURI</button>
          <button id="btnDecode" class="btn btn-warning">decodeURIComponent</button>
          <button id="btnSwap" class="btn btn-outline">Swap Input/Output</button>
          <button id="btnClear" class="btn btn-outline-danger">Clear</button>
        </div>
        <div>
          <label for="outputText" class="label-text">Output</label>
          <textarea id="outputText" class="input input-bordered w-full" readonly></textarea>
        </div>
      </div>
    `n

<ToolShell about={aboutText} />

</ToolLayout>

<style is:inline>
.saweria-float { position: fixed; bottom: 24px; right: 24px; z-index: 9999; background: #ff6b6b; color: #fff; border-radius: 50px; padding: 10px 20px 10px 16px; text-decoration: none; font-size: 14px; font-weight: 600; display: flex; align-items: center; gap: 8px; box-shadow: 0 4px 15px rgba(255,107,107,0.4); transition: all 0.3s ease; font-family: "Inter",sans-serif; }
.saweria-float:hover { transform: translateY(-3px); box-shadow: 0 6px 20px rgba(255,107,107,0.6); color: #fff; }
.saweria-float svg { animation: pulse 1.5s ease infinite; }
@keyframes pulse { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.15); } }
@media (max-width: 576px) { .saweria-float { bottom: 16px; right: 16px; padding: 8px 14px 8px 10px; font-size: 12px; } }
</style>

<script is:inline>
</script>

<script is:inline>
var inp = document.getElementById('inputText');
var out = document.getElementById('outputText');

document.getElementById('btnEncComponent').addEventListener('click', function() &#123;
  try &#123; out.value = encodeURIComponent(inp.value); &#125; catch (e) &#123; out.value = 'Error: ' + e.message; &#125;
&#125;);
document.getElementById('btnEncURI').addEventListener('click', function() &#123;
  try &#123; out.value = encodeURI(inp.value); &#125; catch (e) &#123; out.value = 'Error: ' + e.message; &#125;
&#125;);
document.getElementById('btnDecode').addEventListener('click', function() &#123;
  try &#123; out.value = decodeURIComponent(inp.value); &#125; catch (e) &#123; out.value = 'Error: ' + e.message; &#125;
&#125;);
document.getElementById('btnSwap').addEventListener('click', function() &#123;
  var tmp = inp.value;
  inp.value = out.value;
  out.value = tmp;
&#125;);
document.getElementById('btnClear').addEventListener('click', function() &#123; inp.value = ''; out.value = ''; &#125;);
</script>

